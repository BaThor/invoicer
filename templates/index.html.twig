<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}
            WSZYSTKO CZEGO DZIIS CHCE
            {{ form_start(form) }}
                {{ form_row(form.name) }}
                {{ form_row(form.price) }}
            {{ form_end(form) }}
        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
