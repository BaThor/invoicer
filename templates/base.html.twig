<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <div class="row h-100">
            <div class="col-2 aside">
                <h3 class="text-center pt-3 text-white">INVOICER by GRUBY</h3>
                <ul class="nav flex-column nav-pills">
                    <li class="nav-item"><a class="nav-link" href="{{ path('add_invoice') }}">Dodaj fakture</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('add_seller') }}">Dodaj sprzedawce</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('add_buyer') }}">Dodaj nabywce</a></li>
                </ul>
            </div>
            <div class="col-10 content">
                {% block body %}
                
                {% endblock %}
            </div>
        </div>
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
